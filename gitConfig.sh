git config --global user.name "Wucaifa"
git config --global user.email "1044093206@qq.com"
git remote add origin https://github.com/Wucaifa/IMServer.git
git add .                                   # 添加所有文件
git commit -m "Initial commit"              # 提交
git branch -M main
git push -u origin main
